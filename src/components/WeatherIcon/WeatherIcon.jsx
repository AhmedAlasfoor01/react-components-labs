const WeatherIcon = (props)=>{
    return(
        <img src={props.img} alt={props.imgAlt}/> //to recieve the image abd props which only reads and in the imgAlt will display it 

    );

};
export default WeatherIcon;